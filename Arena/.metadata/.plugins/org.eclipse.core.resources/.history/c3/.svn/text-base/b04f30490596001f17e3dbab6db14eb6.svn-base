import java.awt.ComponentOrientation;
import java.awt.Container;

import javax.swing.*;

public class MazeMaker {
	
	private JFrame mainScreen;
	private JLabel label1;
	
	public MazeMaker()
	{
			mainScreen = new JFrame();
			label1 = new JLabel();
			mainScreen.setSize(600, 400);
			label1.setText("Hello, world");
			JLabel label2 = new JLabel();
			label2.setText("layout test");
			label2.setLocation(label1.getPreferredSize().width, 0);

			mainScreen.setComponentOrientation(ComponentOrientation.LEFT_TO_RIGHT);
			mainScreen.add(label1);
			mainScreen.add(label2);
			
			
			
			mainScreen.setVisible(true);
	}
}
